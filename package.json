{
  "name": "ironsheet",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Move fast and break no spreadsheets.",
  "scripts": {
    "verify": "node scripts/verify.mjs",
    "commit:safe": "scripts/commit-safe.sh",
    "hooks:install": "git config core.hooksPath .githooks"
  },
  "engines": {
    "node": ">=18"
  }
}

